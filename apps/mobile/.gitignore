
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Expo cache
.expo/

# Committed native ios/ kaldırıldı — prebuild'e bırakıldı (app.json'dan üretilir)
/ios/

# Push/FCM secrets — repoya commit'lenmemeli
# NOT: google-services.json BİLEREK ignore edilmiyor — EAS git-modu build'inde
# yüklenmesi gerekiyor (bare Android FCM config). Android client config'i düşük
# hassasiyetlidir (APK içinde zaten gömülü). Asıl gizli olan service-account key
# ve APNs .p8 aşağıda ignore'da kalıyor.
GoogleService-Info.plist
*.p8
fcm-service-account*.json
*-firebase-adminsdk-*.json